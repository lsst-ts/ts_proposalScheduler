setupRequired(python)
setupRequired(python_future)
setupRequired(sconsUtils)
setupRequired(ts_dateloc)
setupRequired(ts_astrosky_model)
setupRequired(ts_observatory_model)
# configuration
setupRequired(scheduler_config)
setupRequired(ts_schedulerConfig)
# driver
setupRequired(ts_scheduler)

envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
envPrepend(PATH, ${PRODUCT_DIR}/scripts)
